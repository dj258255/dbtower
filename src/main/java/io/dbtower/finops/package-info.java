/**
 * 비용/효율 인사이트 (D6 FinOps) — AWS FinOps agent·Mydbops(미사용 인덱스 절감) 모델의 축소판.
 *
 * 인스턴스별 "낭비 후보"를 신호로 모아 보여준다: 미사용 인덱스(사용 통계), 중복·잉여 인덱스(D2 재사용),
 * 큰 테이블·과다 인덱싱(크기), 오버프로비저닝(연결·메모리 여유). 온디맨드 REST(GET
 * /api/instances/{id}/finops)와 웹 콘솔 "비용/효율" 카드로 노출한다.
 *
 * 정체성 가드레일(신호까지만): 모든 분석기는 기존 operator 조회 결과만 판정한다(읽고 조언만 — 대상 DB를
 * 바꾸지 않는다). 인덱스 자동 삭제도, rightsizing 실행도 하지 않는다. 실제 클라우드 과금(달러) 연동은
 * 자격증명이 필요해 범위 밖이라, <b>절감액을 지어내지 않고 "절감 가능 신호"까지만</b> 낸다. 적용 불가 기종은
 * UNSUPPORTED로 정직하게 표기한다(통과 위장 금지).
 *
 * 의존은 operator·registry·advisor(중복 인덱스 판정 재사용)로 한 방향뿐이라 모듈 순환이 없다.
 */
package io.dbtower.finops;
