/**
 * DbmsOperator가 주고받는 이기종 공통 값 객체(record) — 모듈 공개 API의 데이터 절반.
 * 서브패키지는 Modulith 기본으로 internal 취급이라, NamedInterface로 노출을 명시한다.
 */
@org.springframework.modulith.NamedInterface("model")
package io.dbtower.operator.model;
