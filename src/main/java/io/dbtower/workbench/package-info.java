/**
 * 거버넌스 SQL 워크벤치 — 사람이 대상 DB를 직접 조회하는 콘솔. 조회는 즉시, 변경은 승인 티켓으로, 위험한 문장은 차단한다.
 *
 * 정체성: 편의 기능이 아니라 정책 계층이다. 분리된 콘솔 계정, 문장 분류, 읽기 전용 실행, 결과 마스킹, 실행 기록을
 * 플랫폼 코드가 강제하고, 사람(과 AI)은 그 안에서만 조회한다. DBeaver·CloudBeaver를 붙이지 않은 이유가 이 계층이다
 * (그 도구들은 DB에 직접 붙어 마스킹·팀 범위·감사를 우회한다).
 *
 * 의존 방향: workbench -> registry(범위·콘솔 계정) · operator(실행) · analysis(리터럴 마스킹). 역방향 참조 없음.
 */
package io.dbtower.workbench;
