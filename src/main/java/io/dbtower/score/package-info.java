/**
 * 통합 헬스 스코어 (Phase D8) — 인스턴스마다 흩어진 신호를 하나의 점수/등급으로 묶어 "어디부터 볼지"를
 * 기계가 우선순위 매긴다. 2026 DBA 설문의 "40%만 통합 모니터링" 고통에 직접 대응한다.
 *
 * 순수 종합 모듈이다: 대상 DB를 조회하지도 바꾸지도 않는다. 이미 만들어진 다섯 신호를 읽어 합산할 뿐이다 —
 * health(registry), 이상 감지(insight D1), Advisors(advisor D2), SLO/에러 버짓(slo D4), 백업 신선도(backup D7).
 *
 * 산식(감점 방식): 100점에서 시작해 신호별 감점을 뺀다. 각 신호의 기여(왜 이 점수인가)를 응답에 분해해 노출한다.
 * 신호 하나의 수집 실패·데이터 부족은 격리한다 — "데이터 부족"(판정 보류·부분 데이터 표기)과 "나쁨"(감점)을
 * 구분해, 없는 신호를 0점/장애로 오판하지 않는다.
 *
 * 의존 방향은 한쪽뿐이다: score는 registry/insight/advisor/slo/backup을 참조만 하고, 아무도 score를 참조하지
 * 않는다 — 그래서 모듈 순환이 생기지 않는다(ModularityTests가 강제).
 */
package io.dbtower.score;
